#include <stdio.h>
#include <math.h>

/*double f(double x)
{
	double result;
	result=1/sqrt(x)-12*cos(x);
	return result;
}

double fonction(double result)
{

}
*/
int main()
{
    int i,n;
    printf("Nombre maximal d'itérations: ");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {   
        printf("compteur %d\n",i+1);
    }
}